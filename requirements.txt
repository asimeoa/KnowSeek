# KnowSeek.ai — Python Dependencies
# Version: rev05_002 — Last updated: 18.03.2026 15:41 — Branch: main_sia05
#
# Note: Ollama is used for TWO functions:
#   01. LLM — llama3:8b — answer generation
#   02. Embedding — nomic-embed-text — text vectorization (DE + EN)
#   Install: brew install ollama
#   Models:  ollama pull llama3
#            ollama pull nomic-embed-text
#
# Note: OCR requires system dependency:
#   brew install tesseract tesseract-lang

# ─────────────────────────────────────────────
# 03 AI & RAG Framework
# ─────────────────────────────────────────────
langchain
langchain-community          # PDF / DOCX / XLSX loaders
langchain-ollama             # Ollama LLM + Embedding integration

# ─────────────────────────────────────────────
# 04 Vector Database
# ─────────────────────────────────────────────
chromadb

# ─────────────────────────────────────────────
# 05 Document Processing
# ─────────────────────────────────────────────
pdfplumber                   # Digital PDF text + table extraction
pymupdf                      # PDF image extraction + metadata (fitz)
pytesseract                  # OCR for scanned PDFs + images
Pillow                       # Image processing for OCR
fpdf2                        # Convert OCR text to searchable PDF
python-docx                  # DOCX parsing
openpyxl                     # XLSX parsing

# ─────────────────────────────────────────────
# 06 Baseline Model
# ─────────────────────────────────────────────
rank-bm25                    # BM25 keyword search — baseline for MLFlow comparison

# ─────────────────────────────────────────────
# 07 API & Web Server
# ─────────────────────────────────────────────
fastapi
uvicorn

# ─────────────────────────────────────────────
# 08 Data Analysis & EDA
# ─────────────────────────────────────────────
numpy
pandas
matplotlib
seaborn                      # Statistical charts, heatmaps
plotly                       # Interactive charts, spider diagram
nbformat>=4.2.0              # Required for Plotly rendering in Jupyter
ipywidgets                   # Required for tqdm progress bars and interactive widgets
# ─────────────────────────────────────────────
# 09 Experiment Tracking
# ─────────────────────────────────────────────
mlflow                       # Local experiment tracking (port 5000)

# ─────────────────────────────────────────────
# 10 Testing
# ─────────────────────────────────────────────
pytest